
%%Prism
L = 161;                % length (m)
B = 21.8;               % beam (m)
T = 15.8;
rho = sym('rho')

m = rho*L*B*T
m = 
%rho = m / (L*B*T)
V = L*B*T

I_zCG = (L^2 + B^2)*m / 12

r_bg = [-3.7; 0; T/2];

I_zCO = I_zCG + rho*L^2*B^2